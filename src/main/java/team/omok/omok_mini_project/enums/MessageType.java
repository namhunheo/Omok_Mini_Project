package team.omok.omok_mini_project.enums;

public enum MessageType {
    JOIN,
    LEAVE,
    ROOM_WAIT,
    ROOM_READY,
    ROOM_END,
    COUNTDOWN,
    GAME_START,
    MOVE,
    GAME_END,
    CHAT,
    ERROR
}
